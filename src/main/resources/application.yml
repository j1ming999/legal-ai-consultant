langchain4j:
  open-ai:
    chat-model:
      api-key: sk-f429b06372234dc980bc05b067ca0985
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      model-name: qwen-plus
      temperature: 0.7
      log-requests: true
      log-responses: true
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: ${API-KEY}
      model-name : qwen-plus
      log-requests: true
      log-responses: true
logging:
  level:
    dev.langchain4j: debug
    dev.ai4j.openai4j: debug

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/consultant?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: consultant-legal-ai-jwt-secret-key-2026
  expiration: 604800000
